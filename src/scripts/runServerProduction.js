/**
 * Скрипт для запуска сервера в продакшен-режиме
 * Устанавливает необходимые переменные окружения и запускает сервер
 */

// Устанавливаем режим продакшена
process.env.NODE_ENV = 'production';

// Загружаем .env файл
require('dotenv').config();

// Запускаем сервер
console.log('Запуск сервера в продакшен-режиме...');
console.log('------------------------------------');
console.log('NODE_ENV =', process.env.NODE_ENV);
console.log('PORT =', process.env.PORT || 3000);
console.log('------------------------------------');

// Импортируем и запускаем сервер
require('../server.js'); 